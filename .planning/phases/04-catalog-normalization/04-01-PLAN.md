---
phase: 04-catalog-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit-catalog-normalization.mjs
  - package.json
  - docs/CATALOG-NORMALIZATION-AUDIT.md
  - docs/CATALOG-NORMALIZATION-AUDIT.json
autonomous: true
user_setup: []
must_haves:
  truths:
    - 歌曲索引中 albumSlug 引用的存在性被自动校验并可重复执行
    - deep-read 与 raw-lyrics 缺失项有可追踪清单
  artifacts:
    - docs/CATALOG-NORMALIZATION-AUDIT.md
    - docs/CATALOG-NORMALIZATION-AUDIT.json
  key_links:
    - scripts/audit-catalog-normalization.mjs -> docs/CATALOG-NORMALIZATION-AUDIT.json
    - scripts/audit-catalog-normalization.mjs -> docs/CATALOG-NORMALIZATION-AUDIT.md
---

# Phase 4 Plan 01: Catalog Baseline Audit

## Objective

- **What:** 建立目录规范化的自动审计脚本与报告
- **Why:** 先获得结构一致性的事实基线，再进行精准修复
- **Output:** 可重复执行的审计脚本 + JSON/Markdown 审计报告

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-catalog-normalization/04-CONTEXT.md
@docs/ARCHIVE-BATCHES.md
@docs/PROGRESS-SNAPSHOT.md
@docs/ISSUES-LOG.md

## Tasks

<task type="code">
  <name>Implement catalog normalization audit script</name>
  <read_first>
    scripts/check-deep-read-quality.mjs
    content/songs/index.json
    content/albums/index.json
  </read_first>
  <files>scripts/audit-catalog-normalization.mjs</files>
  <action>新增 Node 脚本，校验 songs/albums 索引关系，输出以下维度：missingAlbumSlug、albumSlugNotFound、duplicateSongSlug、missingDeepRead、missingRawLyrics、unusedAlbums；并把结果写入 docs/CATALOG-NORMALIZATION-AUDIT.json 与 docs/CATALOG-NORMALIZATION-AUDIT.md。</action>
  <acceptance_criteria>
    scripts/audit-catalog-normalization.mjs contains "albumSlugNotFound"
    scripts/audit-catalog-normalization.mjs contains "missingRawLyrics"
    scripts/audit-catalog-normalization.mjs contains "CATALOG NORMALIZATION AUDIT"
  </acceptance_criteria>
</task>

<task type="code">
  <name>Wire npm script and run audit</name>
  <read_first>
    package.json
    scripts/audit-catalog-normalization.mjs
  </read_first>
  <files>package.json; docs/CATALOG-NORMALIZATION-AUDIT.json; docs/CATALOG-NORMALIZATION-AUDIT.md</files>
  <action>在 package.json scripts 增加 "audit:catalog": "node scripts/audit-catalog-normalization.mjs"，执行一次审计命令并落盘输出两份报告文件。</action>
  <acceptance_criteria>
    package.json contains "audit:catalog"
    docs/CATALOG-NORMALIZATION-AUDIT.json exists
    docs/CATALOG-NORMALIZATION-AUDIT.md exists
  </acceptance_criteria>
</task>

## Verification

- Run: `npm run audit:catalog`
- Validate report files exist and include summary counts
- Confirm JSON includes all required issue buckets

## Success Criteria

- [ ] 一条命令可重复生成目录一致性审计结果
- [ ] 审计报告能直接用于 Phase 4 后续修复分派
